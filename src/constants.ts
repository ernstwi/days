import path = require('path');

export const markdownOptions = { html: true, breaks: true };
export const templateDir = path.join(__dirname, '..', 'templates');
export const staticDir = path.join(__dirname, '..', 'static');
export const assetDir = 'assets';
