extends ../main

block content
    .stat
        each monthData, year in data
            .stat-header!= year
            each dayData, month in monthData
                .stat-header!= dateFormat(new Date(0, month-1), 'mmmm')
                .list
                    each n, day in dayData
                        .item
                            .title!= day
                            .bar
                                .fill(style=`width: ${max == 0 ? 0 : (n/max)*100}%`)
                            .value!= n
