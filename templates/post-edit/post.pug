extends ../post

block content
    form(class='post-body edit' id='main' action=`${back}?edited` method='post')
        textarea(form='main' name='message')
            != fs.readFileSync(file).toString().replace(/\n$/, '')

block action
    input(type='submit' form='main' value='Done')
